<Window x:Class="Zunify.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zunify" Height="150" Width="350" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Row="0" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions></Grid.ColumnDefinitions>
            <Label Content="Selected File" HorizontalAlignment="Center"  VerticalAlignment="Center" />
        </Grid>
        
        <Button Grid.Row="1" Click="ChoosePlaylistClick" Margin="12">
            <Label Content="Choose Playlist" />
        </Button>
        <Button Grid.Row="1" Grid.Column="1" Margin="12">
            <Label Content="Parse and Save" />
        </Button>
    </Grid>
</Window>
